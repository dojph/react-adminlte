import React from 'react';

export default class DropdownButtonDescription extends React.Component {
    render() {
        return (
            <p>
                DropdownButton component is a contextual overlays for displaying lists of links and more.
                It is a button that, when clicked, it will show its sub-buttons.
            </p>
        );
    }
}