import React from 'react';
import Box from 'doj-react-adminlte/Box';

export default class DropdownButtonDescription extends React.Component {
    render() {
        return (
            <Box>
                <Box.Body>
                    DropdownButton component is a contextual overlays for displaying lists of links and more. It is a button that, when clicked, it will show its sub-buttons.
                </Box.Body>
            </Box>
        );
    }
}