import React from "react";

export default class AlertDescription extends React.Component {
    render() {
        return (
            <p>Avatar component is used in managing a user's profile picture. </p>
        );
    }
}