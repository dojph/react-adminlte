import React from "react";

export default class AlertDescription extends React.Component {
    render() {
        return (
            <p>Avatar is commonly used in managing a user profile picture. </p>
        );
    }
}