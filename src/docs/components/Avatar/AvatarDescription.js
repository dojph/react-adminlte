import React from "react";
import Box from "doj-react-adminlte/Box";

export default class AlertDescription extends React.Component {
    render() {
        return (
            <Box>
                <Box.Body>
                    Avatar component is used in managing a user's profile picture. There are two types of Avatar, user initials and image.
                </Box.Body>
            </Box>
        );
    }
}