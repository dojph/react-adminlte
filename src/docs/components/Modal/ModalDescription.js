import React from 'react';

export default class ModalDescription extends React.Component {
    render() {
        return (
                <p>The Modal component is a dialog box/popup window that is displayed on top of the page. Modal is commonly used in showcasing a login form, videos, images, and alert popups. </p>
        );
    }
}