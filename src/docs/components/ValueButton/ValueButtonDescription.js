import React from 'react';

export default class ValueButtonDescription extends React.Component{
    render(){
        return (
            <p>Value Button is one of the most used component because it allows the users to interact with web pages, take various actions and move from one webpage to another.</p>
        )
    }
}