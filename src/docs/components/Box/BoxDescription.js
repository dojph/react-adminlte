import React from "react";

export default class BoxDescription extends React.Component {
    render() {
        return (
            <p>The box component is the most commonly used component because you can use it for displaying blocks of text. You can also change the style of the box so that the box will become elegant </p>
        );
    }
}