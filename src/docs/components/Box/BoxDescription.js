import React from "react";

export default class BoxDescription extends React.Component {
    render() {
        return (
            <p>Box component is used to keep things more organized and attractive. It is recommended to use BoxHeader, BoxBody, and BoxFooter in a Box. </p>
        );
    }
}