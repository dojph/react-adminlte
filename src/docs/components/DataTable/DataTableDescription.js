import React from "react";

export default class DataTableDescription extends React.Component {
    render() {
        return (
            <p>
                DataTable is a powerful component for creating table listings and adding interactions to them.
                It also provides paging, sorting, and filtering without any configurations.
            </p>
        );
    }
}