import React from 'react';

export default class TextAreaDescription extends React.Component {
    render () {
        return (
            <p>
                TextArea is commonly used for creating a form input so that a user can leave a comment or a message.
            </p>
        );
    }
}