import React from 'react';

export default class CheckBoxDescription extends React.Component {
    render() {
        return (
            <p>
                The CheckBox component can be used to let the user select any number of options from a list of preset options.
            </p>
        );
    }
}