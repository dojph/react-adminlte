import React from 'react';

export default class RadioGroupDescription extends React.Component {
    render() {
        return (
            <p>
                RadioGroup lets a user select one option from a list of preset options.
            </p>
        );
    }
}