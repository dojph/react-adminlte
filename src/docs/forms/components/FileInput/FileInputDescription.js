import React from 'react';

export default class FileInputDescription extends React.Component {
    render() {
        return (
            <p>
                FileInput lets the user choose a file from their device storage for uploading or manipulation using the File API.
            </p>
        );
    }
}