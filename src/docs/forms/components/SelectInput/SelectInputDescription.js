import React from 'react';

export default class SelectInputDescription extends React.Component {
    render () {
        return (
            <h4>SelectInput is commonly used to create a drop down list of options</h4>
        );
    }
}