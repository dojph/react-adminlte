import React from 'react';

class SelectInputDescription extends React.Component {
    render() {
        return (
            <p>
                SelectInput returns a single object value from a given array of object options.
                This is useful for updating nested object properties like object relationships.
            </p>
        );
    }
}

export default SelectInputDescription;